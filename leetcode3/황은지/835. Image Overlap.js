/**
 * @param {number[][]} img1
 * @param {number[][]} img2
 * @return {number}
 */
var largestOverlap = function (img1, img2) {
  // 상하좌우로 움직일수있고,
  // 최대한 많이 겹칠때 1의 갯수를 구하라?
  // 9*9매트리스로 그릴려했으나, 움직이는 방향이 중간에 바뀌면 커버가 불가하다.
};
